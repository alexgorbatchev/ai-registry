# Comma-separated list of tools to generate
# `opencode` -> .output/opencode
# `agentsmd` -> .output/agents
# other targets -> generated inside each profile folder
RULESYNC_TARGETS="opencode,agentsmd"
