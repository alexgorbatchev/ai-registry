---
description: Report the current context window budget
---

Use the `context_budget` tool immediately and return its current budget breakdown with minimal rewording.

If the tool reports that no context budget data is available yet, say that clearly and explain that the plugin analyzes the assembled prompt during normal messages, so another message may be needed before the report is available.
